{% extends "base.html.twig" %}

{% block body %}
    <h1>Category create page!</h1>
    
    {{ form_start(form, { attr: {novalidate: 'novalidate'} }) }}
        {{ form_row(form.name) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}
    
    </br>
    </br>
    <a href="{{ path('admin_category_list') }}">
        <button>Show category list</button>
    </a>
{% endblock %}