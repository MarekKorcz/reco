{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ product.slug }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ product.quantity }}</td>
            </tr>
            <tr>
                <th>Images</th>
                <td>
                    {#{% for image in product.images %}
                        {{ image }}
                    {% endfor %}#}
                    
                   {# <a href="{{ asset('uploads/pictures/' ~ product.picture) }}">
                        Show picture
                    </a>#}
                   
{#                   <img src="{{ asset('uploads/pictures/' ~ product.picture) }}" alt="zdjatko" height="142" width="162"/>#}
                </td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ product.category }}</td>
            </tr>
            <tr>
                {#<td>
                    <a href="{{ path('cart_add', {'productId': product.id }) }}">Add to Cart</a>
                </td>
                <td>
                    <a href="{{ path('cart_remove', {'productId': product.id }) }}">Remove product from cart order (temporary presence)</a>
                </td>
                <td>
                    <a href="{{ path('cart_change', {'productId': product.id, 'quantity': 5}) }}">Change item order quantity (temporary presence)</a>
                </td>#}
            </tr>
        </tbody>
    </table>
{% endblock %}